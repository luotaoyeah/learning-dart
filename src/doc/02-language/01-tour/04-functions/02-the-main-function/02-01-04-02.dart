/*
 * https://dart.dev/guides/language/language-tour#the-main-function
 */

/*
 * 每一个应用的入口都是最顶层的 main() 函数,
 * main() 函数接收一个 `List<String>` 类型的可选参数
 */
void main(List<String> args) {
  assert(args.length == 0);
}
